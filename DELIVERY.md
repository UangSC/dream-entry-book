# 入梦书正式三线与账号服务交付

更新日期：2026-09-14。此文替代上一版临时单线交付说明。

## 运行

在 `D:\Projects\Zhihu` 安装前端依赖后运行 `npm run build`、`npm run preview`；另一个终端执行 `.venv\Scripts\python -m uvicorn backend.app:app --host 127.0.0.1 --port 62561`。首次后端安装：`python -m venv .venv`，然后 `.venv\Scripts\python -m pip install -r backend/requirements.txt`。

入口：http://127.0.0.1:62560/ 。前端同源 `/api` 代理到 FastAPI。默认模拟知乎授权码登录，无需知乎密码。部署配置详见 `backend/README.md`。

## 剧情与引擎

- 唯一正文来源为只读 `D:\Projects\最终方案` 的 00–06 七份定稿；快照在 `content/final/`，`sources.json` 保存逐文件 SHA256，编译前检查。没有使用旧丰满版或临时单线正文补写正式剧情。
- 02–05 转为 **45 节点、951 拍、25 旗标、12 个选择点、7 个终幕**。正文与按钮文案没有润色或新增闲话，错字也未擅修。
- 原始行、行号、拍/选项 ID、可见文本对应在 `content/final/import-audit.json`；全路径在 `route-audit.json`，章节名称在 `chapters.json`。
- 三个内部选后节点 `empty-house-after`、`zhang-return-after`、`taoist-danger-after` 用来承载选后原文；共用原章节标题，不额外引入选择点，不重复播放章节标题。C8 的技术标题在界面显示为“第二颗心”，正文与按钮不变；挡火选项按真话/谎话拆成两个互斥跳转，玩家始终只看到一个同文案按钮。
- 最小兼容扩展：可选 `edition: longform`，上限 96 节点、每节点 160 拍；普通包保留 32 节点/20 拍限制。长篇用实际可达状态遍历而非理论 2^旗标数拒绝，状态仍限制 10000。全条件技术节点按每个可达状态验证至少一拍可见。
- 表情演出 `portrait` 改为可选，无图配角显示艺术姓名。师祖只闻声与剑光，未配置立绘。
- 七个终幕注册及 reflections 原样接入选择回响。源稿七处 `outcomes` 均为空，保留空数组。`ending-bitter`/`ending-bitter-echo` 同名双文本在梦册合并，所以列表为六项。

| 线路 | 完整路径数 | 可见正文字数范围 | 最短纯汉字 | 单周目选择数 |
| --- | ---: | ---: | ---: | --- |
| A 清欢 | 64 | 10852–11409 | 9165 | 7 |
| B 酸心 | 32 | 8506–9179 | 7156 | 6 |
| C 万心 | 96 | 11998–12798 | 10061 | 7 或 8 |

字数由确定性引擎逐路径实际回放的可见正文计算，去空白、含标点，不含按钮、标题、UI 和未显示条件拍；另列纯汉字作为更严格参考。三线最短纯汉字也均超过 7000。

定稿总计划写 A 线 6 次/C 线 8 次，与真实矩阵不完全一致；本轮以 02 一致性契约及 06 跳转矩阵为准，未删除或编造选项凑数。

## 音画与界面

- 复用原有八张背景、表情素材管线、DialoguePortrait、StoryChoices、AutoReader、ChapterTitle、choiceMotion、asides 通用能力。正式书不配置 asides，以确保锁定正文完整。
- 六首新曲全部接入：渡灵气·照护期、市侩诙谐、冷与危、酸心线、万人寻她、兑现与送别。剧情中的换景、呼喊、烧山、碎身、供品、院门按实际拍挂点；烧山从冷危到念念入火场才转万人寻她。
- 十二个用户音效全部利用，包括六个短音效/事件素材及火、雨、林雨、林间、庙等环境层。英文资源 ID/路径，MP3/WebP 入正式包；79 素材，约 25.1 MiB。
- 根目录 `new_TMP_SPR` 原件已归档到 `assets/source/art/portrait-originals`，八张全身图经透明处理存 characters/public portraits。小妖怪饥饿表情已检查透明，不带白色底板。
- 无袄用于脊干和 B 线，红袄在送别披袄那一拍才出现；B 线 E1′ 原文明示临终披袄时才转受伤有袄。毓娘、张老爷、七岁念念、成年念念、老道士全身图参与演出。
- 同一角色表情淡出淡入，按情绪段保持，避免一拍后立刻回默认。对白不再显示姓名栏；左侧放大艺术姓名，无图角色也在相同区域显示姓名。
- 艺术字体本地子集 166 字、61904 字节；沿用已有字体许可文件和普通字体回退。
- 章节标题停 3 秒、灰化 3 秒；首章 LOGO 在灰化中点（4.5 秒）开始用 2 秒消失，后续章节不再出现。入梦后 UI 向外收拢，右侧菜单图标竖排，进度条贴底；页脚淡出时对白框进一步下移。
- 立绘压住较矮的对白框上缘；继续只显示箭头；转场跳过按钮已移除。六种情绪选择交互复用，包括犹豫逃跑、害羞扭开后返回；按钮保留键盘可达与减少动态效果降级。
- 自动播放用环形进度，全文出现后计时，到选择自动停；设置含三档自动语速，面板/隐藏页面阻断自动推进。
- 刘看山沿用扩展后的短句和知乎引导：首页每次进入 10 秒内必出现一句随机知乎引导，之后与普通短句混合随机。

## 账号、记录与模拟 Agent

- FastAPI + SQLite WAL：users、plays、jobs、imports。右上圆头像入梦后变小并移到菜单列最上，悬停放大；点击打开最近七天游玩、任务、入架三个页签。
- Authorization Code 模拟链路完整，服务端校验浏览器绑定的 state、一次性短期授权码，同源写操作校验 Origin，HttpOnly/SameSite Cookie；HTTPS 自动 Secure。Token 不进入前端或数据库。
- 后台 asyncio 队列五阶段推进，支持取消、重试、每用户三个活动任务限制和重启恢复。不同重试轮次不会被旧任务完成事件覆盖。
- 任务成功提供可下载、可通过前端严格校验的 `.dreambook`；产物明确标注复用小妖怪定稿的模拟书，未调用真实模型。任务和下载均按登录身份隔离。
- 所有三类列表只查最近七天，最多每类 100 条；启动清理过期记录及产物。打开账号/书架面板暂停 BGM 与环境声，关闭后恢复。
- 后端足迹不等于云存档：完整书签与章节回放仍保存在当前浏览器。服务进程重启需重新登录，历史记录仍在。
- `/api` 不进入 Service Worker 缓存，避免授权页面被离线首页拦截。

## 验收记录

- `npm run build`：通过，45 节点/951 拍/79 素材，包回读校验成功。
- `npm test`：14 个测试文件、376 项全部通过，含新增定稿溯源测试。
- `npx tsx tools/verify-final-story.ts`：192 完整路径、7 终幕、132 条件拍显隐与回放/检查点存档验证通过。
- `npm run check:release`：45 节点及全部素材哈希通过。
- `npm run test:offline`：102 资源断网从缓存读取、网络请求 0；API 跳过缓存、子目录、跨版本、部分缓存场景通过。
- `.venv\Scripts\python -m pytest backend/test_app.py -q`：4 项通过。Starlette 测试客户端有两项第三方弃用提示，无失败。
- 桌面实际 UI：模拟昵称“桌面验收读者”登录，建立“山里的来信”任务，100% 后校验导入成功，入架记录显示；清欢线从头抵达“已得万人心”，分歧回望到“不去”抵达“先欠着”，收藏与回响正常。
- 桌面三线均从第一页走到终幕：A“已得万人心”、B“酸尽回甘”、C“心甘情愿”。另通过分歧回望验证 A“不去”、B“不现身”原版与“谎话＋挡火”版、C“继续”到“妖王不吃鸡腿”。七份终幕文本均实际抵达，梦册六枚收齐；B 同名双文本仍只增加一枚梦签。
- 账号面板已核对三次周目足迹、原模拟任务与入架记录；开启自动阅读时打开面板，拍 ID 和环形进度保持不变，关闭后恢复并在 C8 选择处自动关闭。字幕动画已从验收用的直接显示恢复为原有演出。
- 桌面最终通关页面控制台错误/警告均为 0，未出现素材缺失；全部素材文件和哈希另经发布检查。最终静态构建 `40a70758332d32`，剧情 build `final-48a4194d7897`。
- 本轮不执行移动端适配或移动端测试。

## 待决策清单与已采取方案

1. **真实知乎登录**：CLI 文档可复用 authorize/access_token 及双凭据请求方式；用户资料端点/返回协议未确定，旧记录还出现未回传 state。预留真实模式与环境变量，缺配置返回 503，缺 state 拒绝兑换；待平台确认后联调，未声称真实登录通过。
2. **红袄表情不足**：全项目实际只有四张红袄表情。smug/soft/hungry 暂用无袄同表情；其中用户明确提及的 smug/soft 已按要求回退。旧 `demon-soft.webp`、`demon-hungry.webp` 与 calm SHA256 同为 `f8fe54cfff92…`，并非缺失的真实表情。等待补图后保持现有 ID 替换即可。
3. **音效与夜景占位**：未找到 SPIRIT/CALL 独立原件，先用无语言的轻声合成占位（CALL 为山谷和声，并非角色配音）；小院夜景由现有昼景压暗生成。路径均按正式规格接入，待新素材可直接替换。
4. **根目录重复音频锁定**：`new_BGM/万人寻她.wav` 与归档原件 SHA256 相同。Windows 拒绝删除；只读 Restart Manager 查询确认由网易云音乐进程 35664 持有。没有关闭用户的播放器，故该重复文件暂留，游戏只引用归档加工后的资源。关闭该曲占用后可删除根目录副本。
5. **四处隐含条件**：为符合矩阵，给 zhang-return“他说要回家取钱…”附 scared-him；funeral“念念藏不住…”及“村里一下子炸了…”附 said-truth；crack-money“……我知道是你。”附 faced-zhang；guarding“你蹲在墙头看她晾…”附 lied。仅控制显隐，文字不变。
6. **原稿冲突原样保留**：ending-bitter 的大狗无条件瘸腿与 guarded-all 存在语义冲突；ending-aftertaste 出现两句“对不起”，与人物基准只一次的要求不一致；05“饿了几几百年的”等错字未改。以正文一字不改为准，待统一修订定稿。
7. **终幕 outcomes 为空、选择次数口径不一致**：按定稿登记及矩阵落地，不新增角色结语或额外选择。详见上文字数表。
8. **本地模拟范围**：真实大模型调用尚未接入；单进程内存会话不支持多 worker 共享登录。当前满足本地模拟需求，真实部署再选持久会话和队列方案。

## 旧版与回滚

正式包 `public/books/little-demon.dreambook`；Demo 保持 `little-demon-demo.dreambook`；临时单线备份 `little-demon-legacy.dreambook` 与 `content/drafts/little-demon-legacy.json` 保留。需要续旧临时存档可导入 legacy 包；正式 build 与旧存档隔离，没有删除浏览器旧书签。

- `2985884`：原项目备份。
- `764e1ea`：正式三线七终幕、192 路径、资源归档与基础演出。
- `ecf7db3`：FastAPI 模拟授权、七天记录、可恢复队列、账号 UI 与精准演出修正。
- `47b98ad`：定稿逐行溯源测试、运行配置说明与交付文档。
- 最后一阶段提交同步生成产物和七终幕桌面验收记录。
